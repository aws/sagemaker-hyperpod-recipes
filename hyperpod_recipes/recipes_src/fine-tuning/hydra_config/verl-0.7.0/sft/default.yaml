# @package recipes
# Common defaults for all VERL SFT recipes (verl-0.7.0)
run:
  results_dir: ${base_results_dir}/${.name}
  model_type: verl

trainer:
  devices: 8

training_config:
  mlflow:
    run_id: ''
    tracking_uri: ''
